## Manual Experiment Tracking Table

| Experiment ID      | Model Type          | Hyperparameters        | Preprocessing Steps        | Feature Selection       | Train/Test Split     | Precision    | AUC Score |
|--------------------|---------------------|------------------------|----------------------------|-------------------------|----------------------|--------------|---------------|
| EXP-01             |  Decision Tree       | criterion = 'gini' , max_depth = 5 , min_samples_split = 2, random_state = 100  | Dropped rows where body_mass_g is null 2. 2.	Impute values with the model value in the sex column. 3. 3.	Drop records from the dataframe where the sex column has invalid data. | All features |	80 /20 |	0.83 |	0.9546
| EXP-02             |  Decision Tree       | criterion = 'gini' , max_depth = 5 , min_samples_split = 2, random_state = 100              | Dropped rows where body_mass_g is null 2. 2.	Impute values with the model value in the sex column. 3. 3.	Drop records from the dataframe where the sex column has invalid data.           | All features            | 80/20                |   0.87	          |   0.9656             |
| EXP-03             | KNN     |n-neighbors=5              | Dropped rows where body_mass_g is null 2. 2.	Impute values with the model value in the sex column. 3. 3.	Drop records from the dataframe where the sex column has invalid data.           | All features            | 80/20                |   0.90	  | 0.9663           |
| EXP-04             | KNN      |n-neighbors=1               |Dropped rows where body_mass_g is null 2. 2.	Impute values with the model value in the sex column. 3. 3.	Drop records from the dataframe where the sex column has invalid data.           | All features            | 80/20                |  0.88   |	0.9774          |
| EXP-05             |  KNN        |n-neighbors=5              | Dropped rows where body_mass_g is null 2. 2.	Impute values with the model value in the sex column. 3. 3.	Drop records from the dataframe where the sex column has invalid data.           | All features            | 70/30                |   0.90	  | 0.9483           |
| EXP-06             | KNN        |n-neighbors=1              | Dropped rows where body_mass_g is null 2. 2.	Impute values with the model value in the sex column. 3. 3.	Drop records from the dataframe where the sex column has invalid data.           | All features            | 70/30                |   0.86	  | 0.9602      
| EXP-07            |Logistic Regression       |Solver=”liblinear”             | Dropped rows where body_mass_g is null 2. 2.	Impute values with the model value in the sex column. 3. 3.	Drop records from the dataframe where the sex column has invalid data.           | All features            | 80/20                |   0.91	  | 0.955  |
| EXP-08            |Logistic Regression       |Solver=”lbfgs”             | Dropped rows where body_mass_g is null 2. 2.	Impute values with the model value in the sex column. 3. 3.	Drop records from the dataframe where the sex column has invalid data.           | All features            | 80/20                |   0.91	  | 0.955  | 
| EXP-08            |Logistic Regression       |Solver=”lbfgs”             | Dropped rows where body_mass_g is null 2. 2.	Impute values with the model value in the sex column. 3. 3.	Drop records from the dataframe where the sex column has invalid data.           | All features            | 70/30                |   0.91	  | 0.955  | 
